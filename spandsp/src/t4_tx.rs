//! T.4 image transmit (encoding) support.
//!
//! This module is a placeholder. The T.4 TX API is complex and involves
//! TIFF file handling and multiple compression schemes. A full wrapper would
//! require significant effort and is left for future work.

/// Placeholder for T.4 transmit state.
pub struct T4TxState {
    _private: (),
}
